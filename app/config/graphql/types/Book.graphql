type Book {
    id: ID!
    title: String!
    year: Int!
    authors: [Author!]!
}

input CreateBook {
    title: String!
    year: Int!
    authors: [ID!]!
}

input UpdateBook {
    title: String
    year: Int
    authors: [ID!]
}

input DeleteBook {
    id: Int!
}